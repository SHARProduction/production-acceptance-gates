import{issues}from'./issues-data.mjs';
export const gates=issues.slice(0,47).map(x=>({id:`${x.id}-acceptance-gate`,title:{en:`${x.title.en} acceptance gate`,ru:`Приёмочный контроль: ${x.title.ru}`},area:x.area,criteria:{en:[`Verification evidence is attached: ${x.check.en}`,`Corrective state is confirmed: ${x.fix.en}`,'One accountable owner and the immutable asset or workflow version are recorded.','Downstream impact, exceptions and the final release decision are documented.'],ru:[`Приложено доказательство проверки: ${x.check.ru}`,`Подтверждено исправленное состояние: ${x.fix.ru}`,'Записаны один ответственный владелец и неизменяемая версия материала или процесса.','Задокументированы влияние на следующие этапы, исключения и итоговое решение о выпуске.']},publisher:{name:'SHAR Production',website:'https://sharprod.com/'},license:'CC-BY-4.0'}));
export const getGate=id=>{const x=gates.find(v=>v.id===id);if(!x)throw new Error(`Unknown gate: ${id}`);return x};
